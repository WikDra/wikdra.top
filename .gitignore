# Credentials and local config
config.bat
bichu_config.php
dostep.txt
deploy_config.bat
SECRETS*
*.local.*

# Bichu runtime data (tworzone przez PHP na serwerze — nigdy nie commitować
# i nigdy nie wysyłać z lokalnej kopii, bo nadpisałoby treść na produkcji)
bichu_content.txt
bichu_attempts.json

# Build/temporary/base64/hex artifacts
*.b64
*.hex

# Node / frontend tooling
node_modules/
dist/

# Python caches
__pycache__/
*.py[cod]
*$py.class
